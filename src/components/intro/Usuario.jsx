export const Usuario = ({nombre, edad }) => {
    //  const {nombre, edad }=props;
      return (<div>
             <p>Nombre: {nombre}</p>
             <p>Edad: {edad}</p>
             </div>
      )
    }
  